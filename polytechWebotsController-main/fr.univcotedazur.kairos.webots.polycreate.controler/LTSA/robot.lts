Function = (forwarding -> rightBump -> leftTurn ->Function |
 forwarding -> leftBump->rightTurn->Function|
forwarding->senseWall->backTurn->Function).

Extra = (doSweeping -> moveButton ->stopMoving ->Extra|
doSweeping -> writeButton ->stopWriting ->Extra).

Mutex = (moveButton -> stopMoving -> Mutex
        | writeButton-> stopWriting -> Mutex

).
assert Prop = [] (initial->(<>forwarding))